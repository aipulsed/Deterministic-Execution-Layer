/**
 * @file logger.ts (tools/utils)
 * @description Lightweight logger utility that re-exports the main logger service.
 */
export { logger as default, logger, type LogContext, type LogEntry } from '../../services/logger/logger';
